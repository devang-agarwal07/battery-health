# battery-health

## Battery Health Monitoring System
This is a React + Vite project for monitoring battery health via serial port connected to an Arduino.

## Setup
Run `npm install` and `npm run dev` to start.
